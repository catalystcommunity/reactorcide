apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "corndogs.fullname" . }}-test-connection"
  labels:
    {{- include "corndogs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['http://{{ include "corndogs.fullname" . }}:{{ .Values.service.port }}/healthz']
  restartPolicy: Never
