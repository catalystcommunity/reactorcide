name: with-secrets-example
command: |-
  sh -c 'echo "Token value: $API_TOKEN" && echo "Checking if secret is masked..."'
environment:
  API_TOKEN: "${secret:project/test:API_TOKEN}"
  NORMAL_VAR: plain-value
